{
	"name": "flowleap-plugins",
	"version": "1.0.0",
	"private": true,
	"description": "The FlowLeap Plugin Marketplace — curated, free-to-install skill packs for patent and IP work.",
	"license": "MIT",
	"homepage": "https://github.com/abdullahatrash/flowleap-plugins",
	"scripts": {
		"validate": "node scripts/validate.mjs",
		"validate:fixtures": "node scripts/validate.mjs --test-fixtures",
		"test": "npm run validate && npm run validate:fixtures"
	}
}
